#ifndef PLIKI_HPP
#define PLIKI_HPP
#include<vector>
#include<string>

int wczytajBaze();

int wczytajZamowienie();

int zapiszZamowienie(std::vector <int> Wynik, std::string plik_nazwa);
#endif // PLIKI_HPP
